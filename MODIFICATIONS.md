# 修改声明

> 依 **GNU GPL v3 第 5(a) 条**要求，本文件是修改版的显著声明。
> 本文档中所有内容同时适用于本源码仓库与由它构建出的二进制发布包。

---

## 一、声明

**本作品是 `YGOProUnity_V2` 的修改版，并非原始作品。**

- **原始作品**：YGOProUnity_V2 —— Unity 版 YGOPro v2 客户端
- **原始许可**：GNU General Public License v3（见本仓库 [`LICENSE`](LICENSE)）
- **修改者**：OverClimber（MeiheweitePro 项目）
- **修改时间**：**2026 年 9 月 16 日 至 2026 年 9 月 25 日**
- **修改版名称**：MeiheweitePro（梅赫维特 Pro）

本修改版整体依旧以 **GPL-3.0** 授权，未附加任何额外限制。

---

## 二、修改内容概要

### 新增功能

**1. RD（超速决斗 / Rush Duel）模式**

- 新增模式总闸与卡池双通道，RD / OCG 双模式共用一个客户端
- 新增 RD 独立卡组目录 `deck_rd/`、RD 独立回放目录、RD 独立背景图
- 新增 RD 人机对战（`rd/ai` 数据包 + `AIRoom` 按模式路由）
- 新增 RD 主菜单「资源更新」入口：`rd/update/*.ypk` 热更新（白名单 + staging + 台账）
- 新增 RD 徽标、菜单项按卡池收敛、卡组记忆按模式隔离
- RD 盘面按模式换图（隐藏 RD 用不到的除外区与最左最右两列），阶段条压成四阶段
- RD 极大怪兽（Maximum）：大框与部件档位、展示行重叠修复；本体与左右部件同帧入场后合体；
  极限状态卡面只显示攻击力；手牌上的召唤按钮改称「极大召唤」
- 极大怪兽形态分化：三段部件分别召唤时，本体位显示单体立绘 `closeup/{码}_2.png`，
  合体（极大召唤）时才用宽幅极大立绘；部件离场后跟随本体去它去的区域，孤立的部件归置墓地

**2. 对局撤回（Undo）**

- 人机对局「真撤回」：同种子重开 + 历史入站消息流喂回，停在目标决策点
- 静默重建（不演出、不推进节拍），按下即本地回退画面
- 撤回点只认人工决策点，自动代答不计入

**3. 视角**

- 新增「俯视角（平面图）」开关：相机由 60° 倾斜抬到 90° 正俯视，牌桌变平面图
- 俯视下场地/手牌按视角自适应（区域取景、行带模型、卡上文字与图标角度跟随）
- 对局途中切换视角立即重摆，无需重开

**4. 联机偏好按模式隔离**

- OCG 与 RD 两套联机面板的历史记录、上次选中、上次手输地址改为分键存储

**5. 萌卡天梯接入**

- 主菜单提供账号登录入口，可登录并按竞技 / 娱乐两种天梯进行匹配对战
- 匹配态「回环」：打完一局天梯、匹配中掉线时回到竞技场界面，不再被丢回服务器列表；
  非匹配路径（人机、残局、卡组测试、手动进服）的返回目标不受影响
- 连接层健壮性：连接对象带代际（撤回重开产生的迟到断线事件按代际丢弃）、
  收发各一条常驻线程、连接 / 接收 / 发送三种超时、TCP KeepAlive、
  瞬时 socket 错误退避重试、决斗中空闲心跳

**6. 其他**

- 卡组记牌：悬停查看卡组剩余牌、摊开卡组就地显示真卡面
- 在线数据更新统一走 CDN，支持手动触发与强制重下
- 联机房间密码历史按服务器分桶、方向键前缀过滤填入
- AI 子进程输出留证与卡死现场快照（排查用）
- 主菜单右下角「支持作者」入口

### 修改的部分

- `Assets/SibylSystem/`：核心逻辑大量改动（房间、对战、AI、设置、更新、界面、连接层）
- `Assets/YGOSharp/`：网络与内核交互适配
- `Assets/Editor/`：构建脚本（图标写入、包体处理）
- `ProjectSettings/`：Unity 版本由 5.6.7 升至 **2021.3.45f1c2**，渲染、图标、输入等设置相应调整
- UI 布局、字体、缩放、图标与文案
- `texture/duel/`：新增 RD 专用场地 / 阶段条派生贴图与极大怪兽框贴图
  （由 `devtools/make_rd_field.py`、`make_rd_phase.py`、`make_rd_mp1.py`、`make_rd_maxband.py` 生成）

### 移除的部分

- 移除了原有的蓝奏云增量包更新链路（统一改走 CDN）
- 移除了开屏 Unity Logo（可配置）

### 新增文件

- `devtools/`：RD 数据解包与打包工具、RD 盘面/阶段条贴图生成器、界面验收脚本、RD 数据布局文档

---

## 三、许可继承说明

因原始作品以 GPL-3.0 授权，本修改版**整体继承 GPL-3.0**，包括：

1. 可以自由使用、修改、再分发
2. 再分发时必须一并提供**对应源码**与**本许可全文**
3. 必须保留原始版权声明与修改声明（即本文件）
4. 不得附加任何额外限制

本工程中**不属于** GPL-3.0 授权范围的部分（Unity Asset Store 插件、卡片数据与卡图等）已在 [`THIRD-PARTY.md`](THIRD-PARTY.md) 中单独说明。

---

## 四、对应源码

本仓库即为发布版二进制的对应源码。

**发布包使用者请注意**：如需获取源码，见发布包内 `授权与来源说明.txt` 中给出的地址；若该地址失效，请通过发布渠道索取。
